# 第一部分 Python 学习指导（第 1–2 周）

> 依据《Agent应用开发工程师-12周冲刺规划》制定。本部分覆盖第 1 周（Python 基础）与第 2 周（Python 进阶与 API 入门），以**检验点一**为终点：命令行聊天机器人可运行，且能说清每一行的作用。

---

## 〇、范围与目标

| | 内容 | 产出物 | 过关标准（规划原文） |
|---|---|---|---|
| 第 1 周 | Python 基础 | 环境与 Git 就绪；基本语法过关；两个小脚本（批量文件处理、Excel 汇总） | 徒手编写 100 行脚本不查语法 |
| 第 2 周 | Python 进阶与 API 入门 | 类、异常、虚拟环境；命令行聊天机器人（多轮对话与流式输出） | 机器人可运行，且能说清每一行的作用 |

第 2 周末为**检验点一**。任一检验点延误超过 3 天，压缩当周新内容、优先补齐旧账，不顺延整体节奏。

---

## 一、怎么学（方法与纪律）

以下五条来自规划的原则与纪律，违反任何一条都会导致"看懂了但不会写"：

1. **以写代读**：每天前 0.5 小时读教程复习，后 2.5 小时必须写代码。编码时间占大头。
2. **语法按需补**：不安排"先读完语法书再动手"。带着当天的脚本任务去查教程相应章节，用不到的跳过。
3. **代码必须亲手写**：AI（包括我）只允许①讲解概念 ②审查你写好的代码 ③出练习题。练习代码不得由 AI 代写、不得用补全工具生成。VS Code 中关闭 Copilot 类自动补全。
4. **卡壳分级求助**：单点卡壳 30 分钟以上 → 问我概念（不问答案）；某任务卡壳超过 3 天 → 降级求助，直接要提示。不做无谓死磨。
5. **每天 commit，周日周记**：所有代码当天 `git add + commit`（这是 Git 练习本身）；周日晚写约 200 字周记（解决了什么、踩了什么坑、学到什么机制），存到 `周记.md`，它就是将来的面试素材库。

---

## 二、在哪里学（资源清单）

**教程（主资源，按需查阅而非通读）**
- 廖雪峰 Python 教程（中文、免费）：https://www.liaoxuefeng.com/wiki/1016959663602400
- Python 官方教程中文版（权威备查）：https://docs.python.org/zh-cn/3/tutorial/index.html
- 交互式闯关练习（可选加餐）：https://futurecoder.io （有中文界面）

**第 1 周脚本相关**
- openpyxl 官方文档（Excel 读写）：https://openpyxl.readthedocs.io

**第 2 周 API 相关**
- openai Python SDK（兼容 DeepSeek / 智谱等）：https://pypi.org/project/openai/
- DeepSeek API 文档：https://api-docs.deepseek.com/zh-cn/ （注册即充，几块钱够练几周）
- 智谱 GLM API（备选）：https://open.bigmodel.cn/
- 实习公司可用的 API key 也可用，但注意合规，不用于项目数据

**我（你的验收人）**
- 随时贴代码给我：我审查、指出问题、追问设计原因
- 概念不懂随时问：我讲解、举例
- 需要练习题：说"出题"，我按你的薄弱点出
- 周末验收：流程见第六节

---

## 三、第 1 周任务清单（D1–D7，从 2026-09-23 开始）

每天节奏：0.5h 读教程对应章节 → 2.5h 写当天练习。**练习代码全部亲手敲。**

### D1（周三）：环境收尾 + 第一个程序
- 教程章节：变量与数据类型、字符串、输入输出（廖雪峰"Python 基础"前几节）
- 环境：本机已有 Python 3.12 与 Git，确认 `python --version`、`git --version` 可用；未装 VS Code 则安装并装 Python 扩展
- 在 `D:\zp\python-learning` 中 `git init`（已帮你建好）
- 练习：
  1. `hello.py`：用 `input()` 问姓名和出生年份，打印问候语和年龄（f-string）
  2. `temp.py`：温度转换器，摄氏 ⇄ 华氏，循环运行直到用户输入 q
- 当天 commit

### D2（周四）：数据结构
- 教程章节：list、tuple、dict、set
- 练习：
  1. `contacts.py`：内存通讯录（dict 存储），支持添加、查找、删除、列出全部，while 循环做菜单
  2. 用 list 存 10 个成绩，手写循环求平均分、最高分、不及格名单（不许用 sum/max 之外的内建偷懒——先手写，再对比内建版本）
- 当天 commit

### D3（周五）：流程控制与函数
- 教程章节：条件判断、循环、函数定义/参数/返回值
- 练习：
  1. `guess.py`：猜数字游戏（随机数、循环、提示大小、统计次数）
  2. `fib.py`：写函数返回斐波那契前 n 项；再写一个判断质数的函数；main 里组合使用
- 当天 commit

### D4（周六，5h 大块）：文件与异常 + 脚本①
- 教程章节：文件读写（with open）、os / pathlib 目录遍历、try/except
- **产出脚本①：批量文件处理（文件整理器）**，要求：
  - 扫描指定文件夹，按扩展名把文件分类移动到 `图片/ 文档/ 压缩包/ 视频/ 其他/` 子目录
  - 支持 dry-run 模式（只打印将执行的移动，不真动文件）
  - 移动过程写入 `整理报告.log`（时间、原路径 → 新路径）
  - 重复文件名冲突要处理（自动重命名 `xxx(1).ext`）
  - 全程 try/except 包裹，任何单个文件失败不中断整体，最后打印成功/失败统计
- 先造一个 20+ 各种文件的测试文件夹再跑
- 当天 commit

### D5（周日，3h）：脚本② + 周复盘
- 教程章节：openpyxl 快速上手（读 sheet、遍历行）
- **产出脚本②：Excel 汇总器**，要求：
  - 读取指定文件夹下所有 `销售明细_*.xlsx`（先用脚本生成 3~5 个模拟文件：姓名、日期、金额、品类）
  - 合并全部记录，输出：每人销售额汇总、每月销售额汇总、金额 Top5 记录
  - 结果写入 `汇总报告.xlsx`（两个 sheet：明细合并 + 汇总统计）
- 写第一篇周记（约 200 字）到 `周记.md`
- 当天 commit

### D6（下周一）：查漏加固
- 把 D1–D5 中"看懂了但写得磕巴"的知识点重写一遍
- 练习：不看任何资料，重写通讯录或猜数字（这是验收预演）

### D7（下周二）：第 1 周验收日
- 找我验收，流程见第六节。核心考核：**限时徒手写一段约 100 行的小脚本，不查语法、不用 AI**

---

## 四、第 2 周任务清单（D8–D14）

### D8：类与对象
- 教程章节：类、实例、`__init__`、方法、继承（理解即可，不深入元类等）
- 练习：把第 1 周的通讯录改写成 `ContactBook` 类（方法：add / find / delete / list_all），main 里实例化使用

### D9：异常深化 + 模块 + 虚拟环境
- 教程章节：自定义异常、raise、模块与包、`if __name__ == "__main__"`
- 实操：`python -m venv .venv` 并激活；`pip install openai`；理解为什么每个项目要独立环境
- 练习：给脚本②加自定义异常 `InvalidSheetError`，格式不对的 Excel 文件抛出并在 main 捕获

### D10：HTTP 与 JSON
- 教程章节：requests 库、JSON 格式
- 练习：调一个免费公开 API（如 Open-Meteo 天气），把返回 JSON 解析打印成可读摘要（城市、温度、风速）

### D11：大模型 API 入门
- 注册 DeepSeek（或用智谱/公司 key），`.env` 文件存 key（学习 python-dotenv），**key 绝不写进代码、不 commit**（`.gitignore` 加 `.env`）
- 用 openai SDK 发第一条消息；理解：`base_url`、`api_key`、`messages` 列表、`role`（system/user/assistant）、返回结构（`choices[0].message.content`）、token 是什么
- 练习：`chat_once.py` — 单轮问答脚本，带异常处理（网络错、key 错、余额不足分别捕获）

### D12（周六，5h）：聊天机器人主体
- **产出：`chat.py` 命令行聊天机器人**，要求：
  - 多轮对话：维护 `messages` 历史列表，每轮把全部历史发给 API
  - 流式输出：`stream=True`，逐块打印不换行，模拟打字机效果
  - 特殊命令：`exit` 退出、`clear` 清空历史、`save` 把对话导出为 txt
  - system 提示词可配置（让机器人有人设，如"你是一个毒舌但专业的编程导师"）
  - 完整异常处理与友好报错

### D13（周日）：收尾 + 周复盘
- 重构：把 API 调用封装成类 `LLMClient`（构造时传 key/model，方法 `chat(messages) -> str` 和 `chat_stream(messages)`）
- 写第 2 篇周记
- 自查：随机打开 `chat.py` 任意一行，能否不假思索说出作用？不能就再过一遍

### D14：检验点一验收
- 找我验收：运行演示 + 逐行解释 + 现场改造（流程见第六节）
- 通过后进入第 3 周（工具调用 / function calling）

---

## 五、过关标准（细化为可核对项）

**第 1 周过关，需全部满足：**
- [ ] 环境：python / git / VS Code 可用，学习仓库已 init 且有每天 commit 记录
- [ ] 脚本①文件整理器：5 项功能全部实现（分类移动、dry-run、日志、重名处理、异常统计）
- [ ] 脚本②Excel 汇总器：多文件合并 + 两维度汇总 + Top5 + 输出 xlsx
- [ ] **限时徒手考核**：我出题，90 分钟内不查语法、不用 AI，写出 80–120 行可运行脚本
- [ ] 两篇脚本代码经我审查，我提出的每个问题你能答上"为什么这样写"

**第 2 周过关（检验点一），需全部满足：**
- [ ] `chat.py` 可运行：多轮对话、流式输出、exit/clear/save 命令、人设配置、异常处理
- [ ] key 通过 `.env` 管理，未进 git 历史
- [ ] **逐行解释**：你对着代码逐行讲作用，我随机抽 10 行追问（为什么用 list 不用 dict、这里为什么要 copy、stream 的 chunk 是什么结构等），答对 8 成以上
- [ ] **现场改造**：我临时加一个小需求（如"再保留最近 10 轮历史防止超长"），你 20 分钟内独立改完并跑通
- [ ] 两篇周记已写

---

## 六、验收流程（怎么找我）

1. **平时**：任意时刻贴代码 + 你的疑问，我按"审查人"标准回复：指出问题 → 追问设计原因 → 必要时出针对性练习。卡壳 30 分钟以上的概念随时问。
2. **周验收（D7、D14）**：你说"第 N 周验收"，流程固定三步：
   - **代码审查**：你贴出本周全部脚本源码 + 运行输出（截图或文本）
   - **答辩**：我针对代码提 10 个左右问题，你逐个回答
   - **限时考核**：我现场出题，你限时徒手写（第 1 周）或现场改造（第 2 周），写完贴回来
3. **判定**：全部通过 → 我明确宣布"第 N 周过关，进入下一周"；有未达项 → 我给出具体的补齐清单和期限（最多 3 天），补齐后重验该项。
4. 验收记录我会追加到本目录 `验收记录.md`，作为你 12 周冲刺的过程凭据。

---

## 七、本阶段常见坑（提前打了预防针）

- **教程刷完≠会写**：廖雪峰教程是用来查的，每天读不超过 0.5 小时
- **AI 依赖**：让 AI 写了练习代码 = 白学，面试白板直接露馅（规划纪律一）
- **抄运行成功的代码不复盘**：每个脚本写完必须能回答：入口在哪、数据怎么流转、为什么这样设计
- **key 泄露**：API key 进了 git 历史就视为泄露，重来
- **Excel 脚本用 pandas 一把梭**：第 1 周先用 openpyxl 手写循环，pandas 第 6 周项目期再系统学（先裸写后框架）
- **周日不核对标准就翻篇**：未达标必须压缩下周新内容补齐，不许带病前进
